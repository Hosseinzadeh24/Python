# Install Python & VS Code + First Program | نصب پایتون و VS Code + اولین برنامه

Chapter 01 — code files shown on screen in the video.

- `hello.py`
  ```
  $ python hello.py
  Hello from VS Code!
  ```
- `hello_v2.py` — version 2 of `hello.py` as typed on screen
  ```
  Hello, world!
  Python 3 13
  2 + 2 = 4
  ```
- `errors.py`
  ```
  before
  caught: name 'prnt' is not defined
  ```

### IDLE session (typed live, not a file)
```
Python 3.13.9 (tags/v3.13.9, 2025) [MSC v.1943 64 bit (amd64)] on win32
Type "help", "copyright", "credits" or "license" for more information.
>>> print("Hello, world!")
Hello, world!
>>> 2 + 2
4
```
